---

- name: "install packages"
  homebrew:
    name: "{{ item }}"
    state: present
  with_items:
    - ansible
    - autoconf
    - bash-completion
    #- boot2docker
    #- docker
    #- elasticsearch17
    - gettext
    - git
    - gpg
    - htop
    - hub
    - iperf
    - libdvdcss
    - libevent
    - packer
    - python
    - nvm
    - ssh-copy-id
  #  - cowsay
  #  - ios-sim
    - readline
    - subversion
    - kdiff3
    - openssl
    - pv
    - tmux
  #  - drush
    - wget
    - wrk
    - zsh
    #- brew-cask
  tags: setup

- name: set permissions
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ webdev_user }}"
    group: "{{ webdev_group }}"
    mode: u=rwX,g=rwX,o=rX
    recurse: yes
  with_items:
    - /usr/local/var/log/
    - /usr/local/var/run/
